import Ember from 'ember';

export default Ember.Controller.extend({
/*
  //trate de llevar este modelo a niveles mas arriba pero no pude asi que todo el controlador puede ser borrado

    data: Ember.computed.alias('model'),  // esta recibe el modelo del route

    sortedFigures: Ember.computed.sort('model.figures', '_figureSort'),
  _figureSort: ['name'],
  //_figureSort: ['name:desc']

actions: {    
    chooseFigure(figure) {     
      this.set('selectedFigure', figure);      
      //alert("entre al chooseFigure de infeatures.js y seleccione figure.name: " + figure.name + ' figure.id: ' + figure.id);           
    },    
  }, 
  */
});    

