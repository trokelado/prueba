<h3>Usuarios</h3>
{{#each filteredToys as |toy|}}
    <h4><li>{{toy.rol}} : {{toy.name}}</li></h4>
{{/each}}

{{#if showMoney}}
  <ul>
    {{#each sortedToys as |eltoy|}}
      {{update-toy toy=eltoy updateToy='updateToy'}}
    {{/each}}
  </ul>
{{else}}  
    Sus privilegios no le permiten visualizar la información operativa. consulte al administrador !
{{/if}}
