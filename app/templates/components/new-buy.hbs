{{#if addNewBuy}}
<h3>Compra Nueva</h3>
  <div>
    <form>
      <div class="form-group">        
        <label for="name">Name</label>
        {{input value=name id="name"}}
    </div>
    <!--div class="form-group">
        <label for="dateIn">Fecha</label>
        {{input type="date" value=dateIn id="dateIn"}}
    </div-->         
    <div class="form-group">
        <label>Instrucciones</label>
        {{textarea class="form-control" cols="80" rows="4" value=instruct}}      
    </div>    
    <div class="form-group">
        <label for="volume">Volumen</label>
        {{input value=volume id="volume"}}        
    </div>      
      <button {{action 'saveBuy'}} class="btn btn-primary btn-sm">Guardar</button>
    </form>
  </div>
{{else}}
  <button {{action 'buyFormShow'}} class="btn btn-primary btn-xs"><span class="glyphicon glyphicon-shopping-cart"></span></button>
{{/if}}
